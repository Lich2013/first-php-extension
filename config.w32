// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("my_string", "for my_string support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("my_string", "enable my_string support", "no");

if (PHP_MY_STRING != "no") {
	EXTENSION("my_string", "my_string.c");
}

